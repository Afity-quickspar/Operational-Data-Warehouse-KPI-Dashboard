# ============================================================================
# Operational Data Warehouse & KPI Dashboard — Python dependencies
# Free / open-source stack (no Snowflake, no Airflow license required)
# ============================================================================

# --- Warehouse engine (Snowflake replacement) --------------------------------
duckdb==1.1.3                 # Embedded columnar OLAP warehouse (SQL-native)

# --- Transformation & testing (dbt) ------------------------------------------
dbt-core==1.8.7               # Data build tool: models, tests, freshness, docs
dbt-duckdb==1.8.4             # dbt adapter for DuckDB

# --- Data engineering / wrangling --------------------------------------------
pandas==2.2.3                 # DataFrame processing for ingestion & KPIs
numpy==1.26.4                 # Numerical backbone
pyarrow==17.0.0               # Parquet exports for Power BI / columnar IO

# --- Synthetic data generation -----------------------------------------------
Faker==30.8.2                 # Realistic customers, orders, events

# --- Self-serve analytics app ------------------------------------------------
streamlit==1.39.0             # Interactive self-serve BI app
plotly==5.24.1                # Rich interactive charts

# --- Orchestration & config (Airflow replacement is custom, pure-python) -----
PyYAML==6.0.2                 # Pipeline configuration
tabulate==0.9.0               # Pretty CLI run summaries
python-dateutil==2.9.0.post0  # Date handling in KPI logic
